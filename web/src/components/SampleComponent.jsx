import { Button } from "@mui/joy";

export const SampleComponent = () => {
  return (
    <div>
      <Button size="md" variant="solid" color="primary">
        Click
      </Button>
    </div>
  );
};
