import { extendTheme } from "@mui/joy";

export const appTheme = extendTheme({
  colorSchemes: {
    light: {
      palette: {
        primary: {
          solidBg: "#2DA44E",
        },
      },
    },
  },
});
